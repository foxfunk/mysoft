<%= form_for(@comentarios_admin) do |f| %>
  <% if @comentarios_admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comentarios_admin.errors.count, "error") %> prohibited this comentarios_admin from being saved:</h2>

      <ul>
      <% @comentarios_admin.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :titulo %><br />
    <%= f.text_field :titulo %>
  </div>
  <div class="field">
    <%= f.label :comentario %><br />
    <%= f.text_area :comentario %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
