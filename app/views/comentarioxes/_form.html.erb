<%= form_for(@comentariox) do |f| %>
  <% if @comentariox.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comentariox.errors.count, "error") %> prohibited this comentariox from being saved:</h2>

      <ul>
      <% @comentariox.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :titulo %><br />
    <%= f.text_field :titulo %>
  </div>
  <div class="field">
    <%= f.label :comentario %><br />
    <%= f.text_area :comentario %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
